#!/usr/bin/env python3
"""Entry point for omaping-store."""
import os
import sys

# The package lives in ../omaping relative to this script.
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))

from omaping.store import main

if __name__ == "__main__":
    sys.exit(main())
